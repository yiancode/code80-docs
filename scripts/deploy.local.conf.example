# 部署配置（复制为 deploy.local.conf 并填写实际值）
# deploy.local.conf 不会提交到 git

REMOTE_HOST="YOUR_SERVER_IP"
REMOTE_USER="root"
REMOTE_DIR="/www/wwwroot/YOUR_DOMAIN"
# SSH_KEY="~/.ssh/id_rsa"  # 可选，指定 SSH 密钥

# Web Server 运行用户（部署后 chown，避免 403）
WEB_USER="www"
WEB_GROUP="www"
